import { PageLoader } from "@/components/ui/page-loader";

// Next.js 15 usará automáticamente este archivo para mostrar el estado de carga en todas las páginas públicas
export default function MarketingLoading() {
  return <PageLoader />;
}
